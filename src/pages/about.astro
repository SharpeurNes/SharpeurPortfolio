---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ImageShow from '../components/ImageShow.astro';
---

<Layout title="About - Sharpeur" description="About me">
    <div class="app-container">
        <Header currentPage="about" />
        
        <main>
            <div class="main-content">
                <h1>About Me</h1>
                <div class="profile-container">
                    <div class="profile-wrapper">
                        <div class="profile-image">
                            <img src="/images/frifri.jpg" alt="Profile Picture" />
                            <div class="shine"></div>
                        </div>
                    </div>
                </div>
                
                <p>I don't really have anything important to say about myself. So here are some pictures of characters / people / things I like.</p>
                <ImageShow></ImageShow>
                <p>And more...</p>
            </div>
        </main>

        <Footer author="Sharpeur" />
    </div>
</Layout>

<link rel="stylesheet" href="/styles/profile-avatar.css" />

<style>
    .main-content h1 {
        font-size: 3rem;
        font-weight: 700;
        margin-top: 2rem;
        background: linear-gradient(135deg, var(--text-light) 0%, var(--primary-purple) 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }
    .app-container {
        display: flex;
        flex-direction: column;
        min-height: 100vh;
    }

    .main-content {
        text-align: center;
        margin-bottom: 4rem;
    }

    .main-content p {
        font-size: 1.2rem;
        color: var(--text-muted);
        max-width: 600px;
        margin: 0 auto;
    }


</style>